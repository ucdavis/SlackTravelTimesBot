const https = require('https');
const config = require('../config/config');
const maps = require('maps');
var Slack = require('slack-node');

function askTravelBot() {
    var slack = new Slack();


}
